<view class="tabbar">
  <view
    wx:for="{{list}}"
    wx:key="index"
    class="tab {{active === index ? 'active' : ''}}"
    data-index="{{index}}"
    catchtap="switchTab"
  >
    <view wx:if="{{index === 1}}" class="tab-center">
      <text class="center-icon">+</text>
    </view>
    <view wx:else class="tab-text">
      <text class="tab-label {{active === index ? 'active' : ''}}">{{item.text}}</text>
    </view>
  </view>
</view>
